{
    "configurations": [
        {
            "name": "Containers: Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/Kubernetes/T04-01 Push Express site to Docker Hub/package.json",
                "localRoot": "${workspaceFolder}/Kubernetes/T04-01 Push Express site to Docker Hub"
            }
        }
    ]
}